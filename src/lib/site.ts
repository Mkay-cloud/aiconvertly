export const SITE_URL = "https://aiconvertly.online";
export const SITE_NAME = "AI convertly";
// Single source for the site-level description -- reused by both the root
// layout's <meta> tags and the homepage's structured data, so the two
// never drift into describing the site differently.
export const SITE_DESCRIPTION =
  "Merge, split, rotate, and convert PDFs and images for free. Every tool runs entirely in your browser — no uploads, no sign-up, no limits.";
